// Types based on leetcode-query response
export type SubmissionStats = {
  difficulty: string;
  count: number;
  submissions: number;
};

export type QuestionCount = {
  difficulty: string;
  count: number;
};
