import { getLeetCodeStats } from "@/utils/lc-stats"


export const LeetCodeStatsCard = async () => {
  let stats = await getLeetCodeStats("idityage")

  return (
    <div>
      hello
    </div>
  )
}
